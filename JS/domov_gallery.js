function scrollDomovGallery(direction){const gallery=document.querySelector('.domov-gallery-thumbnails');const scrollAmount=200;let scrollLeft=gallery.scrollLeft;const maxScrollLeft=gallery.scrollWidth-gallery.clientWidth;if(direction===1){if(scrollLeft+scrollAmount>=maxScrollLeft){gallery.scrollTo({left:0,behavior:'smooth'})}else{gallery.scrollBy({left:scrollAmount,behavior:'smooth'})}}else if(direction===-1){if(scrollLeft-scrollAmount<=0){gallery.scrollTo({left:maxScrollLeft,behavior:'smooth'})}else{gallery.scrollBy({left:-scrollAmount,behavior:'smooth'})}}}
function showDomovImage(src){const modal=document.getElementById('domov-gallery-modal');const modalImage=document.getElementById('domov-gallery-modal-image');modalImage.src=src;modal.style.display='block'}
function closeDomovModal(){const modal=document.getElementById('domov-gallery-modal');modal.style.display='none'}
